# ToDo

This file is a snapshot of pending/in-progress work, kept current automatically by a SessionEnd hook (see `.claude/settings.json`). It reflects current state, not a historical log.

- Once the market reopens: verify `AntOrderNotifyStream`'s core assumption before relying on it in place of REST polling - place a small ANT order, compare the `brokerOrderId` returned by `ANT.placeOrder`/`placeBracketOrder` against the `norenordno` reported in the order-notify WS push for the same order, and confirm they're the same value. If they don't match, `waitForFill()` will silently time out on every order (see `src/ant/AntOrderNotifyStream.ts` header comment). Also confirm the heartbeat fix (`{"heartbeat":"h","userId":...}`) actually keeps the connection alive past the ~5-minute mark that the earlier wrong format was dropping at, and confirm the fill-price field is really `flprc` (assumed from the Noren/Shoonya convention, not yet seen in a live ANT push). This also covers `squareOffOnAnt`'s non-bracket fill-price fix (`src/processes/order/antExecutor.ts`), which reuses the same `waitForFill` mechanism.
- `ContinuousStrategy`'s `logGateOnce` gate-log messages are misleading to a reader unfamiliar with "T1" jargon, and specifically `"T1 gate: already ordered / T1 in flight"` reads as if a real broker order exists when it only means the in-memory `ordered` mutex flag is set (e.g. while `isPcrAligned`'s async PCR fetch is in flight, no order has been placed). Reword these gate messages to be self-explanatory and not imply a live order when there isn't one (see `src/strategy/ContinuousStrategy.ts` around `logGateOnce`/`processNiftyQuote`). Not urgent - noted during 2026-08-25 live monitoring session, no code change made yet.
- Live/manual verification still pending for the 2026-08-25/26 money-correctness/reliability/UX plan work (code is committed, `tsc`-clean, and unit-tested where feasible - these are the parts that need a real running session to check):
  - `AntStream`'s reconnect-with-backoff - drop a live connection and confirm `[AntStream] Reconnecting in 2000ms...` then a successful reconnect.
  - `exitMonitor.reconcileFromTrades` - currently a no-op in practice since nothing populates `bookkeeping.trades` from Mongo/broker at order-process startup; re-verify once a startup trade refresh exists. Note: it now also needs to infer `watchOnly` correctly from `trade.gttTriggerId` (added 2026-08-26, see below) once that startup refresh exists.
  - Frontend UX changes (admin Trades/Payments tabs, square-off confirm+error, NotificationBell reconnect) - no live browser walkthrough was done (Chrome extension wasn't connected in that session) - worth a manual pass through `frontend/`'s `npm run dev` before trusting the UI end to end.
  - `bookkeeping.trades` (open positions) still has no startup refresh from Mongo/broker - only `closedTrades` got this fix (2026-08-26, see below). An open position at the moment of a restart still silently drops out of `/openTrades`/`/positionstream` today.
- 2026-08-26 live P&L/settings-cache/per-user-capital work (code committed, `tsc`-clean on both backend and frontend, and the settings→order-process cache round trip was directly verified via `curl` against the live `order` process) - still needs a real live trade to confirm end-to-end:
  - `exitMonitor`'s new watch-only mode (`src/processes/order/exitMonitor.ts`, `zerodhaExecutor.ts`/`antExecutor.ts` `finalizeEntry`) - on the next `useGTT=true` manual buy, confirm `orchestrator.log` shows `exitMonitor watching ... (watch-only, broker owns exit)`, that the frontend's live P&L actually moves with the market while the position is open (not just at entry/exit), and that no `exitMonitor triggering squareoff` line appears for it (the broker's GTT should be the only thing that closes it).
  - Admin Page's new "Allotted Capital" column (`frontend/src/pages/AdminPage.tsx`) - no live browser walkthrough was done (Chrome extension wasn't connected in this session); the field was only verified via `curl` against `POST /users/:email/settings` plus a direct IPC call to `OrderClient.getUserAllottedCapital`, both of which worked. Worth a manual pass in the browser to confirm the column renders/edits/saves correctly and blank correctly means "use `config.yml`'s strategy default".
  - `ContinuousStrategy.capitalCheck()`'s new per-user `allottedCapital` override (`src/strategy/ContinuousStrategy.ts`) - hasn't been exercised by a real T1 entry yet (PCR/momentum alignment hasn't fired all session); once one does, confirm the "T1 entry skipped - would exceed allotted capital" / "Level N spawn skipped..." logs reflect a per-user override when one is set, and the `config.yml` value otherwise.
- 2026-08-26 (later same session): ANT's OHLC quote endpoint (`getQuote`/`getQuotes` in `src/ant/ANT.ts`) now has a shared 1.2s minimum gap between calls plus one automatic retry-with-backoff on a 429, added after the manual buy above got rate-limited on its very first (and only) call of the session. Live-verified directly against the real AliceBlue API just now: an isolated test call still got a genuine 429 (confirms the account's rate limit is tight/shared regardless of our own call volume - not caused by over-calling), and the new retry logic transparently recovered on the second attempt. Nothing further pending here - this one's confirmed working, not just `tsc`-clean.
- 2026-08-26 (later still): `ContinuousStrategy`'s contract-pricing path (`estimateOptionPricesBatch` in `src/processes/order/antExecutor.ts`) now sources strike premiums/tokens from `ANT.getOptionChain` (new method, `src/ant/ANT.ts`) instead of the rate-limited OHLC endpoint - confirmed no other behavior change needed since it's `getContractByPriceRangeOnZerodha`'s sole caller and the return shape is identical. Live-verified directly: `getOptionChain('NIFTY')` and a full `estimateOptionPricesBatch` call both returned correct strikes/premiums/tokens with zero calls to `chart/get/multi/ohlc` (grepped the test output to confirm). `estimateOptionPrice` (single-contract, manual-buy sizing only) and `enterPosition`'s ANT bracket-order limit-price calc deliberately still use the OHLC path (lower frequency, already covered by the throttle+retry above) - not migrated, per the approved plan's scope. Still needs a real `ContinuousStrategy` T1/spawn entry (hasn't fired yet this session, still PCR-gated) to confirm this path end-to-end inside the live strategy flow rather than via a standalone script.
- 2026-08-26 (later still): fixed the "closed trades tab shows nothing after a restart" gap - `bookkeeping.closedTrades` is in-memory only and was never repopulated on `order` process startup, even though every close is already durably written to Mongo's `closedTrades` collection (`persistClosedTrade`). Added `OrderBookkeeping.loadClosedTradesFromMongo()` (`src/processes/order/bookkeeping.ts`), called from `orderProcess.ts`'s `main()` at startup, which reloads today's realized trades back into memory. Live-verified end-to-end: restarted the stack, confirmed the `[order] Reloaded 1 closed trade(s) from Mongo for today` log line, then confirmed `GET /closedtrades` actually returns the trade with correct fields. Nothing further pending here. Note: `right`/`strategy` come back empty for this specific trade only because the original manual-buy path never set them on the `Trade` object in the first place (`buyContractOnZerodha` in `zerodhaExecutor.ts`) - a separate, pre-existing, cosmetic gap unrelated to this fix, not addressed.
- 2026-08-26 (later still, urgent - live incident): a manual buy on `NIFTY2690124300CE` (qty 715, entry 133.61363636363637 - a multi-fill average) had its GTT rejected by Zerodha: *"Stoploss trigger price should be a multiple of tick size 0.05"*. Root cause: `Zerodha.ts`'s `placeTargetStopLossGTT`/`modifyTargetStopLossGTT` only rounded target/stopLoss to 2 decimals, not to a 0.05 tick multiple - a non-round entry price (common with multi-fill averages) easily produces a 2-decimal price like 122.61 that isn't a valid tick. Compounding it, the GTT-failure catch block in `finalizeEntry` (`zerodhaExecutor.ts`) never registered the trade with `exitMonitor` at all (the watch-only registration added earlier today only runs *after* a successful GTT placement) - so a GTT failure left the position with **zero protection**: no broker bracket, no in-app monitoring, live P&L frozen at 0, would never auto-exit.
  - Fixed: added `roundToTick`/`NFO_TICK_SIZE` (0.05) to `src/zerodha/Zerodha.ts`, applied in both `placeTargetStopLossGTT` and `modifyTargetStopLossGTT` (the latter also used by `POST /prism/settarget`'s manual "Set Target/SL" control, which had the identical latent risk). Also applied to `trade.targetPrice`/`stopLossPrice` in `zerodhaExecutor.ts`'s `finalizeEntry` and (for local-field consistency only, see below) `antExecutor.ts`'s `enterPosition`.
  - Fixed: `finalizeEntry`'s catch block now falls back to full (non-watch-only) `exitMonitor.registerTrade(...)` on a GTT failure, so a broker-side rejection degrades to the same in-app target/SL monitoring `useGTT=false` users already get, instead of leaving the position completely unwatched.
  - Verified: `tsc` clean; directly re-ran the exact failing entry price (133.61363636363637) through the new `roundToTick` and confirmed both resulting prices (135.6, 122.6) are now valid 0.05-tick multiples (the old unrounded stopLoss, 122.61, was not).
  - ANT/AliceBlue path checked but **not changed** for the tick-rounding part: `ANT.placeBracketOrder` sends `targetLegPrice`/`slLegPrice` as raw point offsets, not absolute prices - AliceBlue computes the actual trigger prices server-side from the eventual fill, so there's no absolute price on our side to round, and no equivalent rejection risk from the values currently in `config.yml` (whole-number points). Also, `placeBracketOrder`'s failure mode is structurally safer than Zerodha's: it's called *before* the fill, so a throw there means no trade is ever recorded at all (nothing to leave unprotected) - unlike Zerodha's post-fill GTT attach. No fallback-registration change was needed there for this reason; flagged here in case that assumption needs revisiting later (e.g. if ANT's own tick-size behavior is ever confirmed to differ).
  - Resolved: the `NIFTY2690124300CE` position that predated this fix closed later the same session (target hit, +130) before anyone needed to intervene manually.
  - Live end-to-end verification: **confirmed working**, twice. Two later manual buys on the same contract both got non-round multi-fill-average entry prices (152.41666666666666 and another) and both got valid GTTs placed successfully (rounded correctly to 0.05 ticks) - one closed via target (+1300), one via stop-loss (-5859.75), both exactly at their rounded trigger prices. `POST /prism/settarget` against a non-round price not separately re-tested, but the same `roundToTick` fix covers it.
  - Noticed in passing (not part of this fix, another change in progress concurrently in this session): `src/user.ts` and `src/processes/order/bookkeeping.ts` have gained per-user `targetPoints`/`stopLossPoints` override fields since this fix was written - not touched here, out of scope for this entry.
- 2026-08-26 (later still): added admin-configurable per-user `targetPoints`/`stopLossPoints` override, mirroring the existing `allottedCapital` pattern end to end - `src/user.ts` (User field + `updateUserSettings`), `src/processes/order/bookkeeping.ts` (`UserSettings` field + `getUserTargetPoints`/`getUserStopLossPoints` getters), `src/processes/orderProcess.ts`'s `loadUserLimits` startup preload, `GET /auth/me` and `POST /users/:email/settings` in `server.ts`, `OrderClient.ts`'s type, and a new "Target Pts"/"Stop-Loss Pts" column pair in `frontend/src/pages/AdminPage.tsx`. Resolution order in every entry path (`buyIndexOnZerodha`/`OnAnt`, `manualBuyOnZerodha`'s `buyContractOnZerodha`, `manualBuyOnAnt`) is now: request-level explicit points > this per-user override > `config.yml`'s global `settings.targetPriceDiff`/`stopLossPriceDiff` default. `tsc` clean on both backend and frontend; live-verified the settings round trip via `curl` plus a direct call to the (pure, no-IPC-needed since target/SL resolution happens inside the `order` process itself) `bookkeeping.getUserTargetPoints`/`getUserStopLossPoints` getters - both correct, and correctly fall back to `undefined` for a user with no override. Test values cleared via proper `$unset` afterward (not `null` - see the `allottedCapital` null-crash incident above for why that matters). Still needs: a live manual buy with a per-user override set, to confirm the resolved points actually reach `finalizeEntry` and produce the expected target/stopLoss prices; and a live browser pass on the two new Admin columns (Chrome extension unavailable this session).
- 2026-08-26 (later still): fixed "the stream wasn't updating the price" - all three of the frontend's SSE consumers (`frontend/src/context/TradingContext.tsx`'s `/positionstream`, `frontend/src/components/NiftyTicker.tsx`'s `/niftystream`, `frontend/src/components/NotificationBell.tsx`'s `/notificationstream`) had an identical latent bug: `onerror` gave up permanently after 3 consecutive reconnect failures ("Refresh the page to retry."), with no automatic recovery even once the backend came back up. Today's session caused an unusually large number of rapid backend restarts (many small edits + a concurrent subagent's edits, each triggering the orchestrator's hot-restart), which very likely tripped this limit and froze the position/price displays exactly as reported. Fixed all three to retry indefinitely with a capped exponential backoff (3s, 6s, 12s, ... capped at 30s) instead of ever giving up - a live trading dashboard should always keep trying to reconnect. `tsc` clean on frontend, build deployed. This also resolves the `ToDo.md` item above about `NotificationBell` reconnect needing verification - it turned out to have a real bug, not just be unverified. Still needs a live browser pass to visually confirm reconnection after a backend restart (Chrome extension unavailable this session) - the fix is in place but only verified by reading the code, not observed live.
- 2026-08-26 (later still, correction to the entry above): the SSE reconnect fix was real but turned out NOT to be the cause of "prices not reflecting live" for an open position (user directly verified via raw browser hits to `/niftystream` and `/positionstream` that the connections were fine - `/niftystream` streamed correctly via a direct `curl` test too, ruling out the reconnect theory for that report). The actual bug, found by tracing the code the user pointed at ("the event is emitted on trade, but not when data is received in ant ws" - exactly right): `GET /positionstream` (`server.ts`) only re-pushes a fresh snapshot to already-connected clients via `orderClient.onPositionsChanged()`, which fires solely from trade events (`bookkeeping.ts`'s `_processTradeEvent`/`setTargetStopLoss`, i.e. fill/close/target-SL edit) - never from a live tick. So `exitMonitor.handleOptionTick` was correctly updating `trade.lastTradePrice` in memory (confirmed working - a live trade's GTT fired and closed it correctly mid-investigation), but nothing ever told connected `/positionstream` clients to re-fetch and push that updated value - the browser only ever saw the entry-time snapshot until the next unrelated trade event.
  - Fixed: added `exitMonitor.onPriceUpdate(listener)` (`src/processes/order/exitMonitor.ts`) - a callback-registration pattern mirroring the existing `onExit`, needed because `bookkeeping.ts` already imports `exitMonitor.ts` so the reverse import would be circular. `handleOptionTick` now fires registered listeners (throttled to at most once per 500ms, regardless of tick frequency) right after updating `trade.lastTradePrice`, for both full and watch-only monitored trades. Added a public `bookkeeping.triggerPositionsChanged()` wrapper around the existing private `notifyPositionsChanged()`, and wired `exitMonitor.onPriceUpdate(() => bookkeeping.triggerPositionsChanged())` in `orderProcess.ts`.
  - `tsc` clean; live stack picked up the change via hot-restart with no errors. **Tested against a real position and did NOT fix the symptom** - see the next entry below for why (the actual blocker is upstream of this fix: no ticks are reaching `exitMonitor.handleOptionTick` at all for the traded option, so there's nothing to trigger the new notify hook). This fix is still correct and should be kept - it's necessary but not sufficient.
- 2026-08-27: found and fixed the root cause of the "live option ticks never arrive" mystery above, for the manual-buy-on-Zerodha case at least. `manualBuyOnZerodha` and `buyIndexOnZerodha` (`src/processes/order/zerodhaExecutor.ts`) resolved the contract via `ZerodhaContractMaster`, which returns **Zerodha's own Kite Connect `instrument_token`** - a different numbering scheme than ANT/Shoonya's - and stored that directly as `trade.token`. `exitMonitor.registerTrade` then subscribed that value on ANT's WebSocket (`dataProcess.ts` → `AntDataStream.subscribeOption`), which only understands ANT-native tokens, so it silently subscribed to a nonexistent instrument - permanently frozen price, not a timing/liquidity issue. Confirmed live: a manual PE buy's `trade.token` (`12030466`) didn't exist in either contract master at all, while the correct ANT token for that exact contract (`46994`) does. ContinuousStrategy's T1/spawn entries never hit this because `getContractByPriceRangeOnZerodha` already extracted the ANT-native `antToken` separately.
  - Fixed: added `AntContractMaster.resolveCommonToken(symbol, optionType, selector)` (`src/ant/AntContractMaster.ts`) - a common function mirroring each of Zerodha's three contract-lookup shapes (ATM-by-LTP, exact strike+expiry, nearest-expiry-by-strike) to resolve the ANT-native token instead. Wired into `buyIndexOnZerodha` and both branches of `manualBuyOnZerodha`/`buyContractOnZerodha` so `trade.token` is always the ANT-native/common token; Zerodha's own `instrument_token` is no longer stored anywhere (it turns out nothing in the codebase needed it beyond this - Zerodha order placement and GTT calls only ever use `tradingSymbol`).
  - `tsc` clean. Live-verified end-to-end: after restarting `npm run processes` to pick up the fix, a fresh manual frontend buy (`NIFTY2690124250CE`) resolved token `46995`, confirmed correct against the ANT contract master (`NIFTY01SEP26C24250`).
- 2026-08-27 (later same session): the manual full-stack restart done to deploy the fix above wiped `ContinuousStrategy`'s in-memory `ordered`/cooldown/`legsByToken` state and it immediately fired a duplicate live T1 entry on top of an already-open position. Investigated and planned (not yet implemented - no code changed) three related restart-safety fixes: (1) `src/orchestrator.ts`'s per-child hot-restart watchers don't cover shared dirs (`src/ant/`, `src/zerodha/`, `src/model/`, `src/util/`, `src/prism/`, `src/tools/`, `src/ipc/`, `src/lib/`), so editing them silently restarts nothing; (2) `ContinuousStrategy` has no way to reconcile its in-memory state against `order`'s real open positions after a `strategies` restart (which already happens automatically and often); (3) `order` itself has no startup restore of open positions either (pre-existing gap, already flagged above under "bookkeeping.trades... no startup refresh"), so a full-stack restart defeats (2) too. Full design (exact files, new methods, wiring, verification steps) written to the plan file `~/.claude/plans/task-notification-task-id-bbwtawsng-tas-clever-goose.md` - read that before implementing.
- 2026-08-28: implemented `ContinuousStrategy`'s root-refill drift-cancel per the plan at `~/.claude/plans/distributed-spinning-dusk.md` - `placeRefill` (see below) watches the pending token (`watchToken`) and records the order's `orderId`; `checkRefillDrift` (called from `processOptionQuote`'s no-open-leg fallthrough) cancels and drops the pending re-entry once LTP runs more than `refillCancelDistance` (default 50, added to `config.yml`) points above the resting limit price - no re-place, re-entry only via normal gates. New `Zerodha.cancelOrder` / `cancelOrderOnZerodha` / IPC `cancelOrderZerodha` plumbing added end-to-end, mirroring `placeLimitBuyZerodhaBare`; `pendingLimitOrders.ts` gets `untrackPendingLimitOrder`. `MOCK_BROKER=true node ./dist/test/continuousStrategyTest.js` run directly: 103/103 assertions pass, 0 failures. Still needs a live check once a root target-hit + drift actually occurs - confirm "Refill placed (root)"/"Root refill cancelled - LTP drifted ..." log lines and that the order shows CANCELLED in Kite's own order book.
- 2026-08-28 (later same session): generalized target-hit refill from root-only to every leg, per the plan at `~/.claude/plans/print-tasks-from-todo-md-jiggly-mochi.md`. `PendingReEntry` now carries `isRoot`/`parentLegId`/`parentLevel`; new `isParentAlive` gates a nested leg's refill to only when its own parent is currently a live leg; new `cancelChildRefills` cascade-cancels a leg's own children's pending refills whenever that leg ends up not refilling itself (5x loss, parent-not-alive, or a nested capital-block, which now skips outright with no retry - root still defers/retries via the existing `deferredRootRefill`); `trySpawnLevel` cancels its own leg's shallower (or same-level, on a retrace) pending refills before spawning a new/deeper hedge; `updateTrade` reconstructs the correct root/nested identity on fill and reoccupies the parent's level-slot for a nested refill. `placeRootRefill`/`checkRootRefillDrift` renamed `placeRefill`/`checkRefillDrift` (both were already or are now leg-agnostic). Root's own exact behavior is unchanged (traced/documented in the plan file - every new code path is a no-op unless a nested pending refill actually exists, which was impossible before this change). `tsc --noEmit` clean; full test run (`MOCK_BROKER=true node ./dist/test/continuousStrategyTest.js`, including 6 new tests for the nested-refill/parent-alive/cascade-cancel/deeper-level-cancel/retrace-recross/updateTrade-reoccupy behavior) passes 103/103 assertions, 0 failures. Still needs a live check once a nested leg's own target-hit actually occurs - confirm "Refill placed (nested)"/"No refill - parent closed"/"Pending refill at level N cancelled" log lines match the live chain's behavior.
