# ToDo

This file is a snapshot of pending/in-progress work, kept current automatically by a SessionEnd hook (see `.claude/settings.json`). It reflects current state, not a historical log.

- 2026-09-22 **LIVE INCIDENT - BulkPcrStrategy LPP order rejection**: Strategy enabled at 12:32:47, evaluated PCR=0.730 → call direction, attempted chunked entry buy for NIFTY2692223350CE @ price=139.45 qty=1755 (8 chunks), chunk 1/8 failed with Zerodha LPP rejection: "order price 139.45 > limit price protection 64.30". No fills, all 13975 qty rejected, manual review required. Root cause unknown - either strategy pricing logic off by ~2.2x, or contract master data stale (Zerodha instruments CSV is 32 days old, ANT contract master is 36 days old). See `bugs.md` for full details. **Follow-up required**: run `scripts/download-zerodha-master.sh` to refresh Zerodha instruments, re-download ANT contract master, review `BulkPcrStrategy.resolveEntryRight()` pricing logic, consider pre-order price validation against current LTP.
- Once the market reopens: verify `AntOrderNotifyStream`'s core assumption before relying on it in place of REST polling - place a small ANT order, compare the `brokerOrderId` returned by `ANT.placeOrder`/`placeBracketOrder` against the `norenordno` reported in the order-notify WS push for the same order, and confirm they're the same value. If they don't match, `waitForFill()` will silently time out on every order (see `src/ant/AntOrderNotifyStream.ts` header comment). Also confirm the heartbeat fix (`{"heartbeat":"h","userId":...}`) actually keeps the connection alive past the ~5-minute mark that the earlier wrong format was dropping at, and confirm the fill-price field is really `flprc` (assumed from the Noren/Shoonya convention, not yet seen in a live ANT push). This also covers `squareOffOnAnt`'s non-bracket fill-price fix (`src/processes/order/antExecutor.ts`), which reuses the same `waitForFill` mechanism.
- Live/manual verification still pending for the 2026-08-25/26 money-correctness/reliability/UX plan work (code is committed, `tsc`-clean, and unit-tested where feasible - these are the parts that need a real running session to check):
  - `AntStream`'s reconnect-with-backoff - drop a live connection and confirm `[AntStream] Reconnecting in 2000ms...` then a successful reconnect. Attempted 2026-08-31: couldn't force this from the current environment - `ss -K` (kill the live TCP socket to AliceBlue's WS host) requires root/CAP_NET_ADMIN, not available here, and deliberately didn't try anything riskier (killing the whole `data` process, attaching a debugger to inject a `.close()` call) this close to market open on the user's live stack. Needs either elevated privileges, or doing it by hand (toggle network briefly, or add a temporary debug-only "force close" trigger) in a dedicated window.
  - `exitMonitor.reconcileFromTrades` - no longer a no-op as of 2026-08-31 (`bookkeeping.trades` now gets a startup restore from the broker/Mongo, see the 2026-08-31 restart-safety entry below); needs live re-verification with a real open position and valid broker session to confirm it actually reconciles and correctly infers `watchOnly` from `trade.gttTriggerId`. Still not exercised as of 2026-09-03: the 3 real overnight NIFTY positions restored that day (see below) all had no `targetPrice`/`stopLossPrice`/`gttTriggerId` on their Mongo Buy docs (ContinuousStrategy manages its own exits via `legsByToken`, not exitMonitor's static target/SL watch), so `reconcileFromTrades` correctly skipped registering them - confirmed by design, not a bug, but means this line is still unverified against a real target/SL-bearing restored trade (e.g. a BuySellStrategy position, or a `useGTT=false` ContinuousStrategy trade if that combination is ever used).
- 2026-09-03: fixed three gaps in broker-position reconciliation found via a live incident (Zerodha session was stale at `order`-process startup, `loadOpenTradesFromBroker` failed silently, and 4 real open Kite positions - including a non-NIFTY one, AMBER26SEP8200CE - went untracked in `bookkeeping.trades`/`exitMonitor`/`ContinuousStrategy` even after a same-session re-login, because nothing re-ran reconciliation after login succeeded):
  1. `src/processes/order/bookkeeping.ts`: split `loadOpenTradesFromBroker` into `reconcileZerodhaPositions()`/`reconcileAntPositions()` (both still called together at startup), added an `isNiftyOption()` filter (tsym starts with `NIFTY`, ends with `CE`/`PE`) so only plain NIFTY-index options are ever restored - excludes BANKNIFTY/FINNIFTY and any stock option like AMBER that isn't this app's concern - and factored the merge+`exitMonitor.reconcileFromTrades` call into a shared `mergeRestored()`.
  2. `src/processes/orderProcess.ts`'s `reloadSession` IPC handler (fired only from server.ts's OAuth callback handlers, i.e. only after a broker login actually succeeds) now also calls `reconcileZerodhaPositions()`/`reconcileAntPositions()`.
  3. `restoreOneOpenTrade`'s Mongo lookup was wrongly bounded to `entryTime >= startOfDay()` on both the last-Sell and Buy-doc queries - broke any NRML/overnight position held across a restart into the next day (exactly what the live incident was: the 3 real NIFTY legs were bought 2026-09-02, carried overnight, and had zero matching Mongo docs under a same-day window). Removed the day bound entirely (tsym already scopes to one specific contract/expiry, so an unbounded search can't cross-match a different contract) - now searches all-time for the most recent Sell, then all Buys after it.
  `tsc --noEmit` clean. Verified live via the dev server's auto-restart-on-save (not yet via an actual OAuth login round trip): after the fix, all 3 real open NIFTY legs were correctly restored into `bookkeeping.trades` with their true entry price/qty from 2026-09-02's Buy docs, `GET /openTrades` reflects them, and `[ContinuousStrategy] reconcile: restored 3 open leg(s) after restart - blocking new T1 entries until they close` confirms the `strategies` process also now sees and is actively managing them via its own leg-tracking (they carry no `targetPrice`/`stopLossPrice`/`gttTriggerId`, so `exitMonitor` correctly does not also watch them - see the `exitMonitor.reconcileFromTrades` entry above). AMBER26SEP8200CE was silently skipped by the new NIFTY filter (no log line for it, and it has zero Mongo Trade docs anyway - confirmed not an app-placed position). Still needs: an actual live Zerodha/ANT re-login (not just a process restart) to confirm the `reloadSession`-triggered reconcile calls fire correctly through that path.
- 2026-09-03 (later same session): added a NIFTY-expiry-day auto-squareoff, per user instruction ("all open positions should be closed by 3:15pm on Tuesday, which is Nifty expiry day"). `src/util/marketHours.ts`: added `isNiftyExpiryDay()` (day-of-week === Tuesday - chosen over parsing each tsym's own expiry date, since tsym-expiry parsing already has known ambiguity caveats elsewhere in this codebase, see `AntContractMaster.ts`) and `isPastExpirySquareOffTime()` (Tuesday AND >= 15:15). `src/processes/orderProcess.ts`: new 60s-polled `setInterval` in `main()`, gated on `isPastExpirySquareOffTime()`, force-squares-off every trade currently in `bookkeeping.trades` (any user, any strategy) via the same `squareOffOnZerodha`/`antExecutor.squareOffOnAnt` calls the existing drawdown-breach auto-squareoff uses; an `expirySquareOffInFlight` Set (keyed `user:token`) prevents re-submitting a squareoff for a position whose Sell fill hasn't yet landed, and a failed attempt is simply retried on the next tick since the trade stays open. `tsc --noEmit` clean. **Not live-verified** - can't be, without either waiting for an actual Tuesday 3:15pm with a real open position, or temporarily faking the clock/day-of-week in a test run. Worth a dry run before trusting it: e.g. temporarily lower the day/time gate (or monkey-patch `isNiftyExpiryDay`/`isPastExpirySquareOffTime`) with a real (small) open position and confirm the squareoff actually fires and the position closes, then revert.
- 2026-09-03 (later still): fixed `scripts/continuous-strategy-trades.sh` to also show legs restored by `ContinuousStrategy.reconcile()` after a restart (the 3 real overnight NIFTY legs above were invisible in the script's output even after the reconciliation fixes, since `reconcile()` only logs one summary count line, no per-tsym detail, and `orchestrator.log` is truncated on every server restart so it had zero ENTRY/SPAWN history for legs opened in a prior process lifetime). Two changes: (1) `src/strategy/ContinuousStrategy.ts`'s `reconcile()` now calls `this.logLegStatus(leg)` for each restored leg, so a `status: qty=... avg=... target=...` line - normally only emitted after an open/spawn/refill/average-add event - gets emitted once right after reconcile too. (2) `scripts/continuous-strategy-trades.sh`'s awk program now treats that `status:` line as itself sufficient to register/open a tsym in the Open Contracts table (previously only ENTRY/SPAWN/REFILL-FILLED lines did that; a status-only tsym falls into the existing "orphan/partial-log" bucket, which already means exactly this: known-open but no root/parent lineage in this log). `tsc --noEmit` clean; live-verified - after the `strategies` process auto-restarted, the log showed `NIFTY2690823800PE`/`23850PE`/`23900CE status: ...` lines right after `reconcile: restored 3 open leg(s)`, and the script now prints all 3 in "Open Contracts" with correct qty/avg/target/nextLevel/squareOff (Capital Used: Rs 68,055 across 3 legs) instead of "(no open legs)". Nothing pending on this fix itself.
  - Frontend UX changes (admin Trades/Payments tabs, square-off confirm+error) - no live browser walkthrough was done (Chrome extension wasn't connected in that session) - worth a manual pass through `frontend/`'s `npm run dev` before trusting the UI end to end.
- 2026-09-10: implemented the two previously-deferred spawn-tree-lineage items (2026-09-03 entries below have been folded into this one):
  1. **Lineage persistence** (`src/strategy/ContinuousStrategy.ts`): every leg (T1 open, hedge spawn, refill fill) now gets a stable `legRef` (`crypto.randomUUID()`, distinct from `legId`, which is just an in-memory per-process counter) and fire-and-forget-writes `{legRef, token, tsym, parentLegRef, parentLevel, isRoot, createdAt}` to a new `legLineage` Mongo collection via `persistLegLineage()` - deliberately a separate collection, not a new field on the `Trade` fill collection, since refill fills are detected by generic broker-fill-handling code (`updateTrade`) that has no strategy context to enrich a `Trade` doc with at insert time. `reconcile()` now queries `loadLatestLegLineage()` (latest doc per token) and does two passes: pass 1 restores each leg's true `isRoot`/`parentLevel`/`legRef`/`parentLegRef` from its lineage doc (falling back to the old isRoot:true/no-parent guess only when a token has no lineage doc at all - e.g. legs opened before this fix), pass 2 resolves each `parentLegRef` to the correct restored parent's freshly-generated `legId` and re-populates that parent's `childByLevel` slot - this closes the duplicate-hedge-spawn risk the old always-isRoot:true guess had. `tsc --noEmit` clean; deployed live via the `processes` orchestrator's auto-restart (`reconcile()` ran cleanly with 0 open trades, no lineage docs to resolve yet). **Not yet live-verified with a real spawn**: ContinuousStrategy is currently `enabled: false` and no leg has been created since this deployed, so the actual persistLegLineage writes / reconcile() two-pass resolution against a real parent-child pair hasn't been observed live yet - worth confirming once ContinuousStrategy is enabled and a real hedge spawn + restart occurs (check the `legLineage` collection has the expected docs, and `reconcile: lineage parent not found` never logs for a leg that should have resolved).
  2. **Mongo-driven trade-tree tool** (`src/tools/ContinuousStrategyTradeTree.ts`, `npm run tradeTree -- --date YYYY-MM-DD [--time HH:mm] [--user <id>]`): reconstructs the full leg tree (parent/child, P&L, open/closed) for any day and optional cutoff time, straight from `Trade` fills + `legLineage`, independent of `orchestrator.log` (which only covers the current server lifetime). For any token with no `legLineage` doc (i.e. every date before 2026-09-10, since lineage-writing only just started), it falls back to reconstructing "generations" purely from the fill history itself - splitting a token's fills into separate legs whenever the position fully closes and then reopens with no lineage doc marking a real new leg, so two independent open/close cycles on the same contract don't get wrongly merged into one node. Manually verified against real historical data (`--date 2026-09-03 --user ContinuousStrategy`): produced 29 correctly-separated legs (20 closed/9 open, total realized P&L -22,592) from 117 real fills, vs. 15 wrongly-merged legs before the reopen-splitting fix was added. Real, permanent limitation for any date before 2026-09-10: no `legLineage` docs exist yet, so true parent/child spawn relationships for historical dates can never be reconstructed, only flat per-token fill history.
- 2026-08-26 live P&L/settings-cache/per-user-capital work (code committed, `tsc`-clean on both backend and frontend, and the settings→order-process cache round trip was directly verified via `curl` against the live `order` process) - still needs a real live trade to confirm end-to-end:
  - `exitMonitor`'s new watch-only mode (`src/processes/order/exitMonitor.ts`, `zerodhaExecutor.ts`/`antExecutor.ts` `finalizeEntry`) - on the next `useGTT=true` manual buy, confirm `orchestrator.log` shows `exitMonitor watching ... (watch-only, broker owns exit)`, that the frontend's live P&L actually moves with the market while the position is open (not just at entry/exit), and that no `exitMonitor triggering squareoff` line appears for it (the broker's GTT should be the only thing that closes it).
  - Admin Page's new "Allotted Capital" column (`frontend/src/pages/AdminPage.tsx`) - no live browser walkthrough was done (Chrome extension wasn't connected in this session); the field was only verified via `curl` against `POST /users/:email/settings` plus a direct IPC call to `OrderClient.getUserAllottedCapital`, both of which worked. Worth a manual pass in the browser to confirm the column renders/edits/saves correctly and blank correctly means "use `config.yml`'s strategy default".
  - `ContinuousStrategy.capitalCheck()`'s new per-user `allottedCapital` override (`src/strategy/ContinuousStrategy.ts`) - hasn't been exercised by a real T1 entry yet (PCR/momentum alignment hasn't fired all session); once one does, confirm the "T1 entry skipped - would exceed allotted capital" / "Level N spawn skipped..." logs reflect a per-user override when one is set, and the `config.yml` value otherwise.
- 2026-08-26 (later still): added admin-configurable per-user `targetPoints`/`stopLossPoints` override, mirroring the existing `allottedCapital` pattern end to end - `src/user.ts` (User field + `updateUserSettings`), `src/processes/order/bookkeeping.ts` (`UserSettings` field + `getUserTargetPoints`/`getUserStopLossPoints` getters), `src/processes/orderProcess.ts`'s `loadUserLimits` startup preload, `GET /auth/me` and `POST /users/:email/settings` in `server.ts`, `OrderClient.ts`'s type, and a new "Target Pts"/"Stop-Loss Pts" column pair in `frontend/src/pages/AdminPage.tsx`. Resolution order in every entry path (`buyIndexOnZerodha`/`OnAnt`, `manualBuyOnZerodha`'s `buyContractOnZerodha`, `manualBuyOnAnt`) is now: request-level explicit points > this per-user override > `config.yml`'s global `settings.targetPriceDiff`/`stopLossPriceDiff` default. `tsc` clean on both backend and frontend; live-verified the settings round trip via `curl` plus a direct call to the (pure, no-IPC-needed since target/SL resolution happens inside the `order` process itself) `bookkeeping.getUserTargetPoints`/`getUserStopLossPoints` getters - both correct, and correctly fall back to `undefined` for a user with no override. Test values cleared via proper `$unset` afterward (not `null` - see the `allottedCapital` null-crash incident above for why that matters). Still needs: a live manual buy with a per-user override set, to confirm the resolved points actually reach `finalizeEntry` and produce the expected target/stopLoss prices; and a live browser pass on the two new Admin columns (Chrome extension unavailable this session).
- 2026-08-26 (later still): fixed "the stream wasn't updating the price" - all three of the frontend's SSE consumers (`frontend/src/context/TradingContext.tsx`'s `/positionstream`, `frontend/src/components/NiftyTicker.tsx`'s `/niftystream`, `frontend/src/components/NotificationBell.tsx`'s `/notificationstream`) had an identical latent bug: `onerror` gave up permanently after 3 consecutive reconnect failures ("Refresh the page to retry."), with no automatic recovery even once the backend came back up. Today's session caused an unusually large number of rapid backend restarts (many small edits + a concurrent subagent's edits, each triggering the orchestrator's hot-restart), which very likely tripped this limit and froze the position/price displays exactly as reported. Fixed all three to retry indefinitely with a capped exponential backoff (3s, 6s, 12s, ... capped at 30s) instead of ever giving up - a live trading dashboard should always keep trying to reconnect. `tsc` clean on frontend, build deployed. This also resolves the `ToDo.md` item above about `NotificationBell` reconnect needing verification - it turned out to have a real bug, not just be unverified. Still needs a live browser pass to visually confirm reconnection after a backend restart (Chrome extension unavailable this session) - the fix is in place but only verified by reading the code, not observed live.
- 2026-08-31 (later same session): the restart-safety fixes from `~/.claude/plans/task-notification-task-id-bbwtawsng-tas-clever-goose.md` (shared-dir watch coverage, `ContinuousStrategy.reconcile()`, `bookkeeping.loadOpenTradesFromBroker()`, the `legsByToken`/`bookkeeping.trades` collision guard) found a real (minor), still-unfixed gap: editing only `src/strategy/ContinuousStrategy.ts` sometimes restarts all four processes (`order`/`data`/`frontend`/`strategies`), not just `strategies` as intended, because `src/tools/ContinuousStrategyBacktest.ts` imports `ContinuousStrategy` and lives in `src/tools/` - one of the genuinely-shared runtime dirs `order`/`data`/`frontend` all correctly watch, so `tsc -w`'s dependency-aware incremental rebuild recompiling that backtest script as a side effect touches `dist/tools/` and trips every watcher. Reproduced once, did not reproduce on a second identical edit (tsc's incremental dependent-rebuild appears to not refire on every edit - exact conditions unconfirmed). Not fixed - options would be moving the backtest script out of `src/tools/` into its own directory, or accepting the occasional extra restart; flagging for a decision rather than picking one unasked.
- 2026-08-28: implemented `ContinuousStrategy`'s root-refill drift-cancel per the plan at `~/.claude/plans/distributed-spinning-dusk.md` - `placeRefill` (see below) watches the pending token (`watchToken`) and records the order's `orderId`; `checkRefillDrift` (called from `processOptionQuote`'s no-open-leg fallthrough) cancels and drops the pending re-entry once LTP runs more than `refillCancelDistance` (default 50, added to `config.yml`) points above the resting limit price - no re-place, re-entry only via normal gates. New `Zerodha.cancelOrder` / `cancelOrderOnZerodha` / IPC `cancelOrderZerodha` plumbing added end-to-end, mirroring `placeLimitBuyZerodhaBare`; `pendingLimitOrders.ts` gets `untrackPendingLimitOrder`. `MOCK_BROKER=true node ./dist/test/continuousStrategyTest.js` run directly: 103/103 assertions pass, 0 failures. Still needs a live check once a root target-hit + drift actually occurs - confirm "Refill placed (root)"/"Root refill cancelled - LTP drifted ..." log lines and that the order shows CANCELLED in Kite's own order book.
- 2026-08-31 (live incident): GapStrategy's `put` trigger at 09:15 (NIFTY gap -58pts) placed a Zerodha buy on `NIFTY2690124100PE` that never filled - Kite's market-protection converts F&O "market" buys into a LIMIT order (3% band), and this strike's liquidity never traded down to the capped price. `Zerodha.getFillPrice()` timed out after 60s and threw, which aborted the strategy's trade attempt (nothing written to bookkeeping, `/openTrades` stayed empty) but left the LIMIT order resting live at the broker with zero app-side tracking - order `2094270205399588865` was still OPEN when checked directly against Zerodha minutes later. Manually cancelled that specific order (confirmed CANCELLED). Root-caused and fixed two bugs: (1) `Zerodha.cancelOrder` was calling `this.kc.cancelOrder(orderId, 'regular')` - backwards from the Kite SDK's actual `cancelOrder(variety, order_id)` signature - so every cancel attempt in the codebase was silently failing with "Route not found"; this also means `ContinuousStrategy`'s `checkRefillDrift` cancel-on-drift path (added 2026-08-28, never live-verified) has been broken since it was written - fixing this one bug should unblock that too, still needs a live drift event to confirm. (2) `Zerodha.getFillPrice()` now cancels the order itself (best-effort, doesn't mask the timeout error) when it gives up waiting, so a stuck/unfilled order can no longer turn into a silent untracked position - `tsc --noEmit` clean, verified end-to-end against the live stale order above (cancel succeeded, confirmed CANCELLED via `kc.getOrders()`). Still needs: a live re-occurrence of a fill timeout to confirm the new auto-cancel fires from inside the running `order` process (this session's verification called `Zerodha.cancelOrder` directly via a script, not through the actual timeout code path); and the `checkRefillDrift` live check noted in the 2026-08-28 entries above, now actually unblocked by fix (1).
- 2026-08-31 (later still): added a `--allDays <baseDir>` multi-day mode to `src/tools/ContinuousStrategyBacktest.ts` (plus `--verbose`), so it can replay every day under `work/data/backups/<Mon-DD>/csv/` in one run and print a per-day + aggregate summary table, instead of one manual invocation per day. Refactored the existing single-day logic into `runBacktestForDay()` (fresh `ContinuousStrategy` + mock broker per day, no state carried across days) and added `discoverDayFolders()`/`printAggregateSummary()`. `tsc --noEmit` clean; single-day mode verified unchanged against Aug-28 data; `--allDays` verified against all 6 available days (Aug-19/20/21/24/27/28) - runs cleanly, exits properly (no hang), correct per-day + totals table. Nothing pending on the multi-day feature itself.
- 2026-09-01: fixed the PCR-throttle bug flagged above - `ContinuousStrategy.fetchPcrIfDue` (`src/strategy/ContinuousStrategy.ts:309`) now gates on `quote.ltt` (falling back to `Date.now()` when unset, e.g. in `continuousStrategyTest.ts`'s mock quotes) instead of always using `Date.now()`, so the 5-minute recheck window tracks simulated tick time in a backtest instead of real wall-clock time. Also added the missing `getUserAllottedCapital` stub to `ContinuousStrategyBacktest.ts`'s `BacktestOrderClient` (it was throwing inside `ContinuousStrategy.capitalCheck` on every T1 attempt, a separate gap from the per-user-capital feature added 2026-08-26 that the backtest mock never picked up - mirrors `continuousStrategyTest.ts`'s `MockOrderClient.getUserAllottedCapital`). `tsc` clean; `test:continuousStrategy` still 103/103 passing (Test 16/17's real-throttle assertions unaffected since mock quotes never set `ltt`); `--allDays` against all 6 days now goes from 0 trades on every day to 138 total (136W/2L, 98.55% win rate, ₹87,672 P&L) - confirms the fix. Also made `--allDays` default to `/home/karthikeyan/work/data/backups` when no `--allDays`/`--niftyFile`+`--optionFile` flags are passed, so `npm run backtest:continuousStrategy` works with no args (single-day `--niftyFile`/`--optionFile` usage is unaffected by the default).
  - **New gap found while verifying the above (not fixed - out of scope for the PCR ask):** `BacktestOrderClient` also has no `cancelOrderZerodha`, so `ContinuousStrategy.checkRefillDrift`'s cancel-stale-pending-refill path (2026-08-28 drift-cancel feature) throws and is caught/logged ("Root refill cancel failed") during a backtest instead of actually cancelling - non-fatal (doesn't stop the run) but means a backtest may slightly overcount P&L from a stale refill that would have been cancelled in reality. Needs the same treatment as `getUserAllottedCapital` - a no-op/logging stub added to `BacktestOrderClient`.
- 2026-09-03 (later still): live incident - ContinuousStrategy's hedge/refill spawns for `NIFTY2690823800PE`/`23850PE` were failing silently. Root-caused to Zerodha rejecting all buys with "Your account has an ageing debit balance of 28802.55" (InputException, collateral margin for option buying blocked until cleared - broker-side, needs the debit paid down/margin freed, not a code fix). Along the way fixed two related code issues, both `tsc --noEmit` clean but **not yet live-verified**:
  1. `src/processes/orderProcess.ts`: 4 IPC handlers (`buyContractZerodhaBare`, `placeLimitBuyZerodhaBare`, `buyIndex`, `antBuyIndex`) did `return placeOrderWithPendingGuard(...)` instead of `return await ...`, so a rejected buy's error skipped `handleRequest`'s own `catch` and became a true unhandled rejection - no IPC response was ever sent, so `OrderClient` just hung for the full 90s client-side timeout instead of getting the real broker error immediately. **Verified live** at 10:02:43 - the very next spawn attempt after the fix returned the real "ageing debit balance" message instantly instead of timing out. Nothing further pending on this part.
  2. `src/zerodha/Zerodha.ts`: added a buy-halt latch - `buyOption()`/`placeLimitBuyOption()` (the only two entry points every Zerodha buy path funnels through) now set `buyHaltReason` the moment the broker rejects with an "ageing debit balance" message and fail every subsequent buy instantly in-process (no broker round-trip), logging a `[Zerodha] FATAL:` line once. Deliberately never auto-clears (only a process restart resets it) since the app has no way to observe the debit balance actually being cleared. Sell/square-off paths (`squareOffOnZerodha`, `marketSellBareOnZerodha`) call `getKiteConnect().placeOrder()` directly, bypassing both gated methods, so sells remain unaffected by design. **Live-verified 2026-09-03 ~12:05** via `orchestrator.log`: on each of two process restarts, the first rejection logged the one-time `[Zerodha] FATAL: ... halting ALL further buy orders ...` line right after a real `[Zerodha.buyOption] Placing NRML market buy ...` attempt, and every subsequent `ContinuousStrategy` Level-3 spawn retry in that process lifetime failed instantly with `Zerodha buying halted - ...` with no further `Placing NRML` line - confirms no repeat broker calls. Nothing pending on this part; the debit balance itself is still an open broker-side blocker (needs to be cleared with Zerodha before further option buys can succeed) - unrelated to the code fix.
  3. Separately, fixed a real bug found while investigating unrelated `orchestrator.log` spam this session: `src/ant/ANT.ts`'s `fetchOptionChainRows()` was posting to AliceBlue's `getUnderlyingExp` endpoint with `{ underlying }` only, missing the `exch` field the endpoint now requires (`EC9131 Exchange field 'exch' is required and cannot be empty`) - added `exch: 'nse_fo'` to match the sibling `getOptionChain` call. This was causing `estimateOptionPricesBatch` to silently return an empty premium map on every call, which cascaded into `getContractByPriceRangeOnZerodha` throwing `No NIFTY CE/PE contract found ...` on every `ContinuousStrategy` hedge-spawn retry (once per price tick) - both errors were spamming `orchestrator.log`. `tsc --noEmit` clean; live-verified post-restart - zero `no expiry for NIFTY` occurrences since. Nothing pending on this fix.
- 2026-09-03: implement the auth/authorization redesign documented in `security.md` (real Google ID-token verification on login, `requireAuth`/`requireAdmin`/`requireSelfOrAdmin` middleware, and gating the ~45 currently-unauthenticated routes listed there) - deferred since no real users are onboarded yet; revisit before onboarding real users, since KYC/bank data and live order placement are at stake once they are. The Cloudflare-IP-only allowlist (the other half of that session's work) already shipped separately and isn't part of this item.
- 2026-09-10: gap_screener_option_quote.csv - user wants two more columns, `0.5%` and `1%`, simulating a take-profit exit (if the option's intraday high reached bought-price+0.5%/+1%, cell = that % of Capital; otherwise cell = actual EOD P&L = `(Option Quote EOD - Option Quote) * Lot Size`, can be negative), plus a TOTAL-row sum of both new columns, and the win percentage of the `Result` column (`count(Result===true)/count(rows)*100`) written into the TOTAL row's Result cell. **Blocked**: nothing in the codebase currently captures the option's intraday high (`AntBatchQuote.ts`'s `collectLiveQuotes` only keeps `lp`/LTP from ticks, `ANT.ts`'s `getScripQuote` only returns `LTP`) - only the *stock's* `1st15m High`/`1st15m Low` exists today. Investigated AliceBlue's historical-candle endpoint (`.../chart/history`, what pya3's `alice.historical_data(...)` wraps) as a fix since it returns real per-minute OHLC `High` and, being a historical query, could even backfill the already-written 2026-09-09 CSV rows - but 3 things are unconfirmed without a live test: (1) whether it accepts this account's OAuth `userSession` bearer token (pya3 itself uses AliceBlue's classic userId+apiKey flow, already confirmed NOT provisioned for this account), (2) whether NFO's "current expiry only" restriction still covers the 29-Sep-2026 contracts in the existing CSV, (3) exact time-of-day availability window. **User wants this live-tested after market hours today, ~4pm 2026-09-10** - add a small `getHistoricalCandles`-style method to `ANT.ts` (own `axios.create()`, per the existing gotcha) and test against an already-known contract (e.g. `HDFCBANK29SEP26P690`). If it works, use it for the day-high (and backfill 2026-09-09's CSV the same way Lot Size/Capital were backfilled); if not, fall back to the stock's `1st15m High`/`1st15m Low` as the target-hit basis. Full plan at `~/.claude/plans/in-gap-screener-option-quote-two-more-concurrent-eclipse.md`.
- 2026-09-10: implemented a common `BrokerExecutor` interface (`src/processes/order/BrokerExecutor.ts`) unifying Zerodha/ANT/Prism order placement (`buy`/`squareOff`/`cancelOrder`/`getFillPrice`/`getPositions`/`getTrades`/`hasValidSession`), per the design at `~/.claude/plans/in-gap-screener-option-quote-two-more-concurrent-eclipse.md` - GTT-vs-bracket-vs-cover mechanics stay internal to each broker's implementation, contract resolution stays broker-specific and outside the interface. **Explicitly no live verification was done for any of this (per user instruction) - all of it needs live confirmation before being trusted:**
  1. **New: Prism/Shoonya cover order** (`src/prism.ts`'s `placeCoverOrder`/`getOrderFillPrice`/`exitCoverOrder`, `src/prism/RestAPI.ts`'s `place_order` now wiring the previously-commented-out `prd:'H'`/`blprc`/`trailprc` fields) - real new broker capability, never tested against a live Shoonya response. Needs a real small-quantity test confirming: `product_type: 'H'` is accepted at all, `blprc` really is an absolute book-loss price (not a point offset - unlike ANT's `targetLegPrice`/`slLegPrice`), the position shows as a genuine cover order at the broker, `exit_order(orderno, 'H')` (`prismExecutor.squareOffOnPrism`) correctly closes it, and that `get_orderbook`'s fill-status/price field names (`getOrderFillPrice` guesses `avgprc ?? flprc ?? avgPrice`) are right.
  2. **New: Prism `getPositions()`** (`prismExecutor.getPositionsOnPrism`, wraps the previously-unexposed `RestAPI.ts` `get_positions`/`/PositionBook`) - field-name mapping (`tsym`/`token`/`netqty`/`netavgprc`/`exch`) is the standard Noren convention but unconfirmed live.
  3. **New: Prism `hasValidSession()`** always returns `true` (no real session-validity concept exists in Prism today) - a real gap, not just unverified; worth a real check being added later if this starts mattering.
  4. **New: ANT `hasValidSession()`** (`ANT.ts`, calls `getPositions()` as a cheap authenticated probe, mirroring `Zerodha.hasValidSession`'s `getProfile()` pattern) - never exercised against a genuinely stale ANT session, so unconfirmed it actually returns `false` when it should.
  5. **Fixed but unverified: `exitMonitor.reconcileFromTrades`'s watch-only inference** (`src/processes/order/exitMonitor.ts`) - previously only checked `trade.gttTriggerId` (Zerodha-only) to decide "broker owns the exit, don't also arm an in-app square-off after a restart", which meant a restored ANT bracket/cover trade (no `gttTriggerId` - that field is Zerodha-specific) was wrongly reconciled as NOT watch-only, risking exitMonitor firing its own square-off in a race with the broker's real bracket/cover exit. Fixed to also check `antOrderNo`/`prismCoverOrderNo`. Found while wiring Prism's new `prismCoverOrderNo` field through the same path (would have hit the identical bug) - **needs a live restart-with-an-open-bracket/cover-position test** (ANT and, once cover order itself is verified, Prism) to confirm the fix actually prevents a double-exit attempt.
  6. **Fixed but unverified: `src/prism/RestAPI.ts`'s `this.username` bug** - `get_time_price_series`/`exit_order`/`get_holdings`/`get_limits` were all sending `uid: undefined` (typo for `this.userId`, which every other method in the file correctly uses) - silently broken before this session, now fixed. `exit_order` in particular is load-bearing for the new cover-order square-off path (item 1 above) - if this fix is wrong or incomplete, cover-order square-off will fail at the broker.
  7. **New: `getBrokerExecutor(userId)` dispatch** (`src/processes/order/brokerExecutors.ts`) now backs `orderProcess.ts`'s `squareOff` IPC handler (replacing its inline `getUserBroker(...) === 'ant' ? ... : ...` branch) - `buyIndex`/`manualBuy` were deliberately left untouched (they do broker-specific contract resolution before ever reaching a `Trade`, which stays outside the interface by design) so this is a small, low-risk change, but still needs one live square-off per broker (Zerodha, ANT) to confirm the new dispatch reaches the same broker the old inline branch did.
  `tsc --noEmit` clean across all of the above.
- 2026-09-10: implemented real auth/authorization (executes `security.md`'s previously-designed-but-unimplemented fix) and fixed the restart-reconciliation user-merge bug found while confirming the multi-user-per-broker model (see `~/.claude/plans/in-gap-screener-option-quote-two-more-concurrent-eclipse.md` for the full design). `tsc --noEmit` clean throughout; per the user's explicit instruction, **no live browser/network-facing verification was performed** - only server-side checks (`tsc`, direct-code unit checks, one Mongo-backed sanity script) described below. Everything needing a real external request or a real multi-user restart is listed as pending here.
  1. **Real auth** (`src/server.ts`): `POST /auth/login` now verifies the Google ID token server-side (`google-auth-library`'s `verifyIdToken`) instead of trusting client-POSTed `email`/`name`/`picture`; `resolveUser()` no longer trusts the `X-User-Id` header or falls back to a tradeable `'Default'` pseudo-user - returns the signed session cookie's value or `null`. New `requireAuth`/`requireAdmin`/`requireSelfOrAdmin('email')` middleware applied across the route set (see point 4). Server now fails closed at startup if `SESSION_COOKIE_SECRET` or `GOOGLE_CLIENT_ID` env vars are unset (verified live: throws immediately with the expected message when unset). **Needs setting these two new env vars wherever the server is actually launched from** (no `.env`/dotenv mechanism exists in this codebase - matches the existing inline-env-var convention) - `GOOGLE_CLIENT_ID` must equal `frontend/.env`'s existing `VITE_GOOGLE_CLIENT_ID` value.
  2. **Loopback bypass** (`src/middleware/cloudflareOnly.ts`'s new exported `isLoopback`, reused by `requestIsLoopback` in `server.ts`): per the user's direct instruction ("localhost need not have any security"), `requireAuth`/`requireAdmin`/`requireSelfOrAdmin` all pass through unconditionally for loopback requests - mirrors `cloudflareOnly`'s own pre-existing trust of loopback (confirmed safe: Cloudflare connects directly to this server's public IP, no local tunnel daemon involved anywhere in this deployment, so `127.0.0.1` can only mean a process on this same machine). `isLoopback`'s IP-range logic unit-verified directly (IPv4/IPv6 loopback, IPv4-mapped-IPv6, real external IPs, undefined - all correct). **Not verified**: an actual non-loopback request being rejected - impossible to produce from this same-machine shell (any local curl is loopback by definition and always passes). Needs one real check from an actual external client (or through the live Cloudflare-fronted domain) confirming a request with no session cookie and a forged `X-User-Id` header gets a 401, not the old (now-removed) header-trust behavior.
  3. **Google login round trip**: the new `verifyIdToken` path itself (right audience, right error handling, `AuthContext.tsx` now POSTs `{credential}` instead of client-decoded fields) has not been exercised via a real Google sign-in - only confirmed the old client-decode code path is fully gone and the request shape matches what the server now expects. Needs one real login through the frontend.
  4. **Route reduction** (`src/server.ts`, 87 → 75 routes): deleted 10 dead/redundant routes (`/test`, `/start` - was `security.md`'s top finding, live order placement reachable by anyone -, `/addTrade`, `/kite/token`, `/ant/token`, `/requestOtp`, `/prism/quick-login` - dead QuickAuth era -, `/ant/order/buy`/`/ant/order/squareoff`/`/ant/order/settarget` - zero callers anywhere in `frontend/src/` or `scripts/*.sh`, confirmed via repo-wide grep); consolidated `/kite/trades`+`/ant/trades` and `/kite/positions`+`/ant/positions` into `GET /broker/:broker/trades`/`GET /broker/:broker/positions` (adds Prism support these never had); renamed the misleadingly-named `/prism/order/buy`/`/prism/squareoff`/`/prism/settarget` (never actually called `prismExecutor.ts`) to `/order/buy`/`/order/squareoff`/`/order/settarget`, updating `frontend/src/context/TradingContext.tsx`'s 4 call sites and rebuilding. `scripts/check-broker-sessions.sh` updated to the new consolidated paths. Live-verified via a standalone server instance (env vars set, `order`/`strategies` IPC intentionally not running): deleted routes correctly fall through to the SPA wildcard (confirmed identical response to a genuinely-made-up path, not a leftover handler); renamed routes reachable (500s were the expected IPC-not-running failure, not 404); `/broker/zerodha/positions` reached real Zerodha API code (real "invalid access_token" error, i.e. the route/dispatch itself works); `/broker/bogus/positions` correctly 400s; `/users` (admin-gated) returned 200 from loopback. **Not verified**: `/broker/ant/positions`/`/broker/prism/positions` end to end against a real session (only the zerodha/bogus cases were exercised in the smoke test above) - low risk since all three paths share the same `getBrokerTrades`/`getBrokerPositions` dispatch function, but worth a real check.
  5. **Fix 2 - per-user restart reconciliation** (`src/processes/order/bookkeeping.ts`): `restoreOneOpenTrade` (merged multiple users' positions on the same broker+tsym into one, attributed to whichever user's Buy was most recent - a known, previously-deferred risk that the multi-user-per-broker model turns from "very low likelihood" into a real, expected scenario) replaced with `restoreOpenTradesForTsym`, which restores one `Trade` per distinct user (also fixing a second latent bug: the last-Sell boundary was previously computed globally across all users for a tsym, not per user). **Verified via a Mongo-backed sanity script** (synthetic docs under an obviously-fake tsym, inserted and cleaned up immediately after - two synthetic users sharing one contract, one with an old fully-closed cycle plus a currently-open multi-fill position, restored correctly as two separate `Trade`s with correct per-user blended price/quantity, old closed cycle correctly excluded). **Noted but out of scope**: the algorithm (both before and after this fix) models a leg's lifecycle as Buy(s)...one full-closing Sell...Buy(s) again - it does not model a *partial* sell against a multi-fill position (a partial sell gets treated as if the whole leg closed, since the last-Sell boundary excludes any earlier Buy). This is a pre-existing characteristic, not introduced by this fix, but worth keeping in mind if partial square-offs become more common under the multi-user model. **Not verified**: an actual live process restart with two real users holding the same real contract via the same real broker - needs a genuine two-user scenario to occur (or be deliberately set up) with a restart timed to catch it.
- 2026-09-11: implemented `maxProfit` for `SupportResistanceStrategy` and extracted `ContinuousStrategy`'s loss-driven hedge-spawn/averaging/refill/maxProfit leg lifecycle into a shared `src/strategy/LegManager.ts` module (per `~/.claude/plans/1-maxprofit-is-added-rustling-platypus.md`), then reused it in `SupportResistanceStrategy`, which also switched to a self-monitored model (own tick-driven leg tracking, no broker GTT) with its entry trigger replaced entirely: it now fires off the dynamic support/resistance breach detector (`src/lib/supportResistance.ts`, driven by `config.yml`'s `srHypothesis:` block, shared with `SupportResistanceHypothesisTest.ts`) gated by the held-duration filter (`heldMinSec`/`heldMaxSec`, currently 407-651s), instead of the old static `supportPrice`/`resistancePrice` config (removed from `config.yml` and `AdminPage.tsx`). Along the way fixed a real bug in the new code: `SupportResistanceStrategy.processNiftyQuote` used `quote.ltt || Date.now()`, which silently substituted real wall-clock time whenever a tick's `ltt` was a legitimate `0` (falsy) - fixed to `??`. `ContinuousStrategy.ts` is now a thin delegator to `LegManager` (PCR/T1-gating logic only); confirmed byte-for-byte behavior-preserving via `npm run test:continuousStrategy` (110/111 assertions pass - **the 1 failure, "spawn succeeds once capital is available" in Test 10, is pre-existing**: confirmed by temporarily reverting to the original pre-refactor `ContinuousStrategy.ts` and re-running - it fails identically there too, since the capital gate is an intentional one-way latch per its own doc comment, contradicted only by that one stale test assertion) and `continuousStrategySellFailure.test.js` (6/6 pass). New `src/test/supportResistanceStrategy.test.ts` (rewritten from scratch, 14/14 pass) and new `src/tools/SupportResistanceStrategyBacktest.js` (+ `backtest:supportResistanceStrategy` npm script, shared CSV/mock-broker helpers extracted to `src/tools/backtestCsvUtils.ts`) - also fixed a real backtest-accuracy gap found along the way: `BacktestOrderClient` never implemented `cancelOrderZerodha`, so a cancelled pending refill order could spuriously "fill" on a later tick in a backtest even after the strategy had already dropped it; added a real cancel. Ran both backtests over all 7 available days (`/home/karthikeyan/work/data/backups`, Aug-19 through Sep-04): ContinuousStrategy 44 trades/86.36% win rate/₹51,831 P&L (maxProfit tripped on 4/7 days); SupportResistanceStrategy 3 trades/66.67% win rate/₹10,354.50 P&L (maxProfit tripped on all 3 days it traded - the held-duration filter is narrow, so entries are rare). `tsc --noEmit` clean throughout; `frontend/src/pages/AdminPage.tsx` updated (new leg-manager fields on both strategies' cards, `Max Profit %` added to both) and rebuilt. **Still needs, before trusting live**:
  1. Promoting the held-duration filter (407-651s) into live/backtested `SupportResistanceStrategy` logic was done per explicit user instruction this session, but it's ahead of the "5 days of data" validation bar the user had previously set for trusting that pattern (see `sr_hypothesis_held_duration_pattern` memory / `SupportResistanceHypothesisTest.ts`'s original 2-day sample) - worth flagging back to the user now that 7 days' worth of backtest data exists, to decide whether to keep/adjust/widen the filter.
  2. No live-market verification was done for either strategy this session - everything above is `tsc`/unit-test/backtest-only, per the task's stated scope. `SupportResistanceStrategy`'s switch to self-monitored (no broker GTT) is a real live-behavior change from before - worth a live smoke test (small quantity) before re-enabling it for real trading (`config.yml`'s `supportResistanceStrategy.enabled` is still `false`).
  3. No live browser walkthrough of the new `AdminPage.tsx` fields (Chrome extension use wasn't requested this session) - worth a manual pass before the next live session.
  4. **Follow-up same session**: re-ran both backtests with `maxProfit: 100` (both strategies, to see behavior with the feature effectively disabled) and caught a real bug this exposed: `LegManager.onOptionTick` cached `leg.lastLtp = ltp` unconditionally, including when `ltp` is `NaN` (~12.5% of historical CSV rows have a literal NaN ltp - a known data gap). `computeCumulativeProfit()` does arithmetic with `lastLtp` (not a comparison), so one NaN tick permanently poisoned that leg's contribution to the cumulative-P&L sum for as long as it stayed open, and `checkAndMaybeTripMaxProfit`'s `if (cumulative < threshold) return;` fails open on `NaN` (any comparison with NaN is `false` in JS) - so maxProfit tripped spuriously, unrelated to real P&L, every time this happened. This bug was inherited verbatim from the original pre-refactor `ContinuousStrategy.ts` (not introduced by the 2026-09-11 refactor above) - it was masked before by the low 2% threshold, since legitimate P&L usually crossed that anyway before a NaN tick could matter; it became obvious at 100% since the spurious trips were the *only* thing tripping it. Fixed: `LegManager.onOptionTick` now guards `if (leg && !Number.isNaN(ltp)) leg.lastLtp = ltp;`. Re-verified `test:continuousStrategy` (110/111, same pre-existing Test 10 failure), `continuousStrategySellFailure.test.js` (6/6), `supportResistanceStrategy.test.js` (14/14) all still pass after the fix. Corrected `maxProfit: 100` backtest results after the NaN fix (0/7 days tripped for either strategy, confirming it) looked suspiciously high (100% win rate both strategies) - user pushed back, correctly. Investigated with `StrategyOrderTree.js` (see below) and found a **second, separate, more consequential bug**, also pre-existing (not introduced by the 2026-09-11 refactor): `src/tools/backtestCsvUtils.ts`'s `BacktestOrderClient.openLeg` (called from `buyContractZerodhaBare`, which `LegManager.tryAverageLevel` also calls to average into an ALREADY-OPEN leg on the same token) unconditionally overwrote the mock ledger's tracked `entryPrice`/`quantity` with just the incremental average-add's own price/qty, discarding the true blended cost basis - so any leg that was ever averaged closed with a wrong (understated qty, wrong entry price) ledger P&L, even though `LegManager`'s own `avgPrice`/`totalQuantity` tracking (`strategy.getStats()`) was correct throughout. Confirmed via the two independently-computed numbers the verbose backtest report already prints side by side ("total P&L (strategy.getStats)" vs "total P&L (ledger cross-check)") disagreeing by ~40% on Aug-19 (18596 vs 26042.25) - the backtest tool's per-day/aggregate summary table was built from the wrong (ledger) number this whole time, for ContinuousStrategy specifically (its design triggers averaging on nearly every adverse-level crossing; SupportResistanceStrategy's much simpler, un-averaged trades were unaffected - confirmed via the tsym==`NIFTY08SEP26P23950` Sep-03 order tree showing 0 average-adds). Fixed `openLeg` to blend price/quantity the same weighted-average way `LegManager.tryAverageLevel` does; re-verified getStats/ledger now agree (18596 vs 18596.5, rounding only) on Aug-19. **Final corrected `maxProfit: 100` numbers**: ContinuousStrategy 59 trades/100% win rate/₹68,861.00 P&L (down from the ledger-bug-inflated ₹82,657.25); SupportResistanceStrategy unchanged at 4 trades/100% win rate/₹71,165.25 P&L (never hit the buggy code path). The 100% win rate itself (both strategies) is very likely a real, explainable structural property of the design at this SL/target ratio - not a further bug: once a leg averages even once, `LegManager.targetFor` collapses its exit target to `avgPrice + postAverageTargetDistance` (default 1 point) which real intraday NIFTY option noise clears almost every time, and `squareOffDistance` (100-500 depending on strategy) is wide enough that the hard stop rarely triggers within one day - worth being skeptical of this in isolation (no slippage/spread/liquidity-constraint modeling in the backtest, only 7 days of data, and it inflates ContinuousStrategy's trade count/P&L the more it averages) but it is not obviously a bug the way the two above were. **Also delivered this follow-up**: fixed `src/tools/StrategyOrderTree.ts`'s regexes (several were stale against the current `LegManager.ts` log wording - `Square-off`/`Refill placed`/`Refill filled` didn't match at all, and `MAX PROFIT TRIPPED`/`Max-profit close`/`CAPITAL GATE TRIPPED`/`External/manual sell` had no regex at all since they predate those features) by extracting a shared `src/tools/legOrderTreeCore.ts` engine and adding a parallel `src/tools/SupportResistanceOrderTree.ts` (+ `orderTree:supportResistance` npm script) - both tools now also work against a saved backtest run's captured stdout, not just orchestrator.log/server.log, since LegManager's log lines are identical in both contexts. **Still pending**: `config.yml` currently has `maxProfit: 100` on both strategies (left from this diagnostic run, not reset back to `2`) - decide with the user what the real intended value should be before this matters for live trading (both strategies are still `enabled: false`).
- 2026-09-09: implemented the gap-screener daily pipeline (consolidated `src/tools/gapscreener/`, new `gapscreener:generate`/`gapscreener:coverorder` npm scripts, cron entries at 10:05am/3:25pm weekdays via `scripts/gapscreener-morning.sh`/`gapscreener-eod.sh`) and ANT cover-order plumbing (`ANT.placeCoverOrder`, `enterPosition`'s new `orderMode: 'cover'` branch, `squareOffOnAnt`'s BO/CO distinction, IPC wiring end to end), gated OFF by default via `config.yml`'s `gapScreenerCoverOrderEnabled: false`. First live 10:05am cron run (2026-09-09) confirmed the pipeline works end-to-end: screener found 16 gap-up/56 gap-down stocks, `gapscreener:start` resolved 17/19 recommended contracts and wrote live quotes, `gapscreener:coverorder` correctly no-op'd. That run also caught and fixed a real bug: `GapScreenerGenerate.ts`/`GapScreenerOptionQuote.ts`/`GapScreenerOptionQuoteEod.ts`/`GapScreenerCoverOrder.ts` originally resolved their CSV paths via `path.join(__dirname, ...)`, which in compiled code resolves to `dist/tools/gapscreener/` (the build-output tree), not `src/tools/gapscreener/` alongside the TS source as intended - the first run's CSVs landed in `dist/`. Fixed by anchoring to `path.join(process.cwd(), 'src', 'tools', 'gapscreener', ...)` instead (safe since every gapscreener npm script and both cron wrapper scripts always run with cwd at the repo root); today's real CSVs were moved from `dist/` into `src/tools/gapscreener/` by hand. `tsc --noEmit` clean after the fix. **Do not flip `gapScreenerCoverOrderEnabled` to `true` against a real account** without first live-testing `ANT.placeCoverOrder` in isolation (small quantity, during market hours) and confirming: the real AliceBlue response shape (`brokerOrderId` field, whether `orderComplexity: 'CO'` is even accepted), whether AliceBlue rejects MARKET orders for CO the same way it does for BO (if so, `enterPosition`'s cover branch needs the same LTP+1% marketable-limit workaround the bracket branch already has), and that `exitBracketOrder(orderNo, 'CO')` actually closes a CO position correctly - mirrors the existing unverified `placeBracketOrder`/`AntOrderNotifyStream` caveat above. Also worth confirming on the next (3:25pm) run: `gapscreener-eod.log` shows `gapscreener:end` correctly reading/writing both CSVs from the now-fixed `src/tools/gapscreener/` location.
- 2026-09-17: implemented ICICI Breeze as a fourth broker end to end - `src/breeze/Breeze.ts`/`BreezeContractMaster.ts` (session auth, quotes, order placement, contract/ATM resolution), `src/processes/order/breezeExecutor.ts` (full `BrokerExecutor` conformance), dispatch wiring (`brokerExecutors.ts`, `bookkeeping.getUserBroker`), IPC (`breezeBuyIndex`/`breezeSquareOff`), and admin HTTP routes (`/breeze/order/buy`, `/breeze/order/squareoff`, `/broker/breeze/trades`+`/positions`). **Live-verified twice this session**: a real buy+sell round trip (NIFTY 23300 CE, then 23300 PE) through the actual running `order` process, correct fills, correct bookkeeping P&L (+₹217.75 on the second one), correct broker-side position closure confirmed via `/broker/breeze/positions`. Confirmed via ICICI's own official docs (not a guess): bracket/cover orders are NOT supported - "Placement, modification, or cancelation of Margin and Option Plus orders via the Breeze API is prohibited" - so every Breeze buy is deliberately a bare/unprotected order; no follow-up needed on this specific point, it's settled.
  - Added `src/breeze/BreezeStream.ts` (live price-tick WS streaming, `/breeze/connect`+`/breeze/stream` routes) and `src/breeze/BreezeOrderNotifyStream.ts` (push-based order-fill notifications, wired into `breezeExecutor.ts`'s `waitForBreezeFill` with a REST-polling fallback - mirrors `antExecutor.ts`'s `AntOrderNotifyStream`+REST-fallback pattern exactly). Auto-connects in the `order` process at startup and on `reloadSession` (`connectBreezeOrderNotifyIfSessionValid`, mirroring `connectAntOrderNotifyIfSessionValid`).
  - 2026-09-18: live-verified Breeze WS price ticks (item 1 below) and implemented a full per-broker live-tick pipeline off the back of it. **Item 1 resolved**: a standalone scratch script (`Breeze.getInstance().wsConnect()` + `subscribeFeeds({stockToken: '4.1!<token>', getExchangeQuotes:true, getMarketDepth:false})`) confirmed 65 real ticks for the live `NIFTY23300PE` position (token `56994`) with the SDK-source-derived shape holding exactly: LTP is `tick.last` (not `ltp`), token is recoverable from `tick.symbol.split('!')[1]`, `ltt` is a formatted date string. Found and fixed a real bug along the way: `Breeze.wsConnect()` synchronously captures `self.userId`/`self.sessionKey` into the socket.io `auth` object at connect time, but `self.userId` is only populated by `ensureSessionRestored()`'s later `apiUtil()` call - calling `wsConnect()` before the session is confirmed restored fails the handshake with "Credentials Missing" and the socket never recovers. Fix: always call `Breeze.hasValidSession()` (forces the restore) before `wsConnect()`.
  - Built on this to wire Breeze's WS into the actual strategy-engine tick pipeline (previously ANT-only, which structurally can't tick Breeze-executed legs - `breezeExecutor.ts` stores `Trade.token` as `BreezeContractMaster`'s ICICI Security-Master token, a different numbering space from ANT's NFO tokens). New `src/ipc/feedSource.ts` (broker→feed lookup table: `zerodha`/`ant`→`ant`, `breeze`→`breeze`, `prism`→`prism` for later), `src/processes/data/DataStream.ts` (shared interface), `src/processes/data/BreezeDataStream.ts` (mirrors `AntDataStream.ts`, subscribes via the `stockToken` form to bypass a separate confirmed SDK bug - see below). `dataProcess.ts` now runs a registry of DataStreams instead of one hardcoded `AntDataStream`; `tokenRouter.ts`'s watch/trade-holder maps are now keyed by `${source}:${token}`, source resolved per-strategy from its own `config.yml` `broker:` field (`Strategies.getExpandedConfig`), defaulting to `ant` - a true no-op for every non-Breeze strategy. `OptionQuote.fromBreeze` implemented for real (was a dead commented-out stub). **Live-verified end-to-end 2026-09-18**: `ContinuousStrategy` (routed `broker: breeze`) reconciled its real open `NIFTY23300PE` leg on restart, `registerTrade` correctly resolved source=`breeze`, and `[BreezeDataStream] subscribed: 56994` confirmed the live subscribe fired with the right token. `tsc --noEmit` clean. **Not yet observed**: a real target/SL/max-profit trip driven off this new feed (the live position was sitting well below both target and square-off through this session) - worth a check next time price moves near either level, or `maxProfit`/target actually trips, to confirm `LegManager` really reacts to the new tick source end-to-end, not just that ticks are subscribed.
  - Also confirmed, separately, a real (not yet fixed) bug in `breezeconnect`'s structured-params `subscribeFeeds` path (no `stockToken`): `self.stockScriptDictList` is only populated by the SDK's own `generateSession()`/`getStockScriptList()`, which `Breeze.ts` deliberately never calls (avoids `traderweb.icicidirect.com` resetting the connection - see `Breeze.ts`'s `generateSession()` comment) - so that path would throw `TypeError: Cannot read properties of undefined` if ever exercised. `BreezeDataStream.ts` (new, above) avoids this entirely by always using the `stockToken` form. `src/breeze/BreezeStream.ts` and its `/breeze/connect`+`/breeze/stream` debug SSE endpoint (frontend process only, not part of the strategy pipeline) still use the broken structured-params path and were NOT fixed this session (deliberately out of scope - `BreezeDataStream.ts` is the real, wired-in path now) - low priority since nothing depends on that debug endpoint working.
  - **Item 2 (`BreezeOrderNotifyStream.ts`'s order-id field mapping) is now CONFIRMED BROKEN, not just unverified**: a real manual Breeze buy this session (`NIFTY23300PE`, order `202609183800006930`) showed `waitForFill failed ... - falling back to REST fill check: ... did not complete within 30000ms (order-notify)`, followed by a successful REST-fallback fill confirmation. The push genuinely never arrived/matched - `orderReference`'s guessed mapping to `placeOrder`'s `order_id` needs an actual fix (not just live confirmation), though the REST fallback continues to make this non-blocking for order placement itself.
- 2026-09-21: **Code review required before trusting a fix (see CLAUDE.md's "Code Review Before Declaring a Fix Done")** - a live-verified BulkPcrStrategy run this session (via Breeze, then again via a new Zerodha broker path) surfaced two real bugs, both now code-CHANGED but **not yet actually code-reviewed** (two `/code-review` attempts both reviewed the wrong scope - see below):
  1. **Config staleness, fixed generically**: changing `BulkPcrStrategy.broker` (breeze -> zerodha) in the Config UI and saving didn't take effect on the already-running `order`/`strategies` processes (same staleness class as an earlier `enabled`-flag-only fix this session). Added `Strategies.syncFromConfig()` (`src/strategy/strategies.ts` - refreshes `expandedConfigs` + each live strategy's `enabled` flag in place, deliberately without rebuilding `this.list`/losing live position state) and `loadUserLimits()` re-run via new `reloadUserLimits` IPC (`src/processes/orderProcess.ts`), both now called unconditionally from `POST /config` (`src/server.ts`) after every save, replacing the old `enabled`-only diff logic. New IPC plumbing: `src/ipc/strategiesProtocol.ts`/`StrategiesClient.ts` (`syncFromConfig`), `src/ipc/orderProtocol.ts`/`src/processes/strategies/OrderClient.ts` (`reloadUserLimits`).
  2. **Stuck Breeze square-off, real trading loss**: BulkPcrStrategy's target-hit exit placed a single static-price limit sell (`squareOffOnBreeze`, `src/processes/order/breezeExecutor.ts`) that never became marketable after price moved away; it sat unfilled, `chunkedSquareOff` timed out at the 90s order-IPC boundary, and the position was left open and unmanaged (`phase='error'`) - user closed it manually. Fixed with a bounded (4-attempt, ~15s each, ~60s total - safely under the 90s IPC timeout) cancel-and-reprice retry loop against a fresh best-bid each attempt (ICICI rejects true market orders for options, confirmed elsewhere in this file, so re-pricing a limit is the only lever). `waitForBreezeFill` gained optional timeout params (defaults unchanged) to make each retry attempt short.
  - **Zerodha/Kite support added to `chunkedBuyIndex`** (`src/processes/orderProcess.ts`, `src/strategy/BulkPcrStrategy.ts`, `src/processes/strategies/OrderClient.ts`) so BulkPcrStrategy can route through Zerodha, not just Breeze - critically resolves the ANT-space `commonToken` (via `AntContractMaster.resolveCommonToken`) rather than Zerodha's own `instrumentToken`, since a zerodha-routed strategy's live option ticks arrive over the ANT feed (`tokenRouter.ts`'s `resolveSource`), not Zerodha's. Live-verified once (full 8-chunk entry, correct qty/avg, no duplicate orders) - the race-condition fix from earlier in this session (`this.phase='buying'` set synchronously before the PCR await) held correctly across both this run and the Breeze run before it.
  - Added a `Broker` dropdown (breeze/zerodha) to `BulkPcrStrategy`'s Config UI (`frontend/src/pages/AdminPage.tsx`, `renderConfigField`'s new `'select'` type) instead of free text.
  - **Update: both `/code-review` attempts failed to scope correctly** (first reviewed nothing-scoped "current diff" in a non-git directory and was killed partway by mistake; second, given an explicit `--files <list>` argument, still returned findings entirely about unrelated, much older code - `--files` does not appear to be a respected argument for this skill/repo). Given a real review still needs to happen before trusting this live per CLAUDE.md's new rule, did a manual line-by-line review instead and found and fixed 4 more real bugs before considering this done:
    1. **Double-sell race in the new square-off retry**: `cancelOrderOnBreeze`'s failure was originally swallowed unconditionally, but a cancel can legitimately fail because the order already filled in the gap between the short wait timing out and the cancel call (a real exchange race). As first written, that would retry with a brand-new sell on top of one that already filled. Fixed: on a cancel failure, do one immediate fill check (`Breeze.getFillPrice(..., 1, 0)`) before looping into a new order; if it turns out filled, use that fill instead of retrying.
    2. **Timeout budget mismatch**: the new per-chunk retry (~60s worst case) can be invoked up to 8 times sequentially for BulkPcrStrategy's full 13975 qty (all within *one* `chunkedSquareOff`/`chunkedBuyIndex` IPC call) - 8x60s could exceed the 90s `OrderClient` timeout that call enforces, which is exactly what caused today's stuck-sell in the first place (a single chunk alone consumed the full 90s with zero retries). Fixed: `OrderClient.chunkedBuyIndex`/`chunkedSquareOff` now use a dedicated 15-minute timeout (covers both the fixed sell side's 8x60s and the still-unfixed buy side's theoretical 8x90s worst case), while every other (single-order) request type keeps the 90s default.
    3. **Config-reload race**: `ConfigService.getConfig()` returns a cached copy only refreshed by `fs.watchFile`'s own polling (~5s interval), and `strategies`/`order`/`frontend` each run their own independent `ConfigService` instance. The new `syncFromConfig()`/`loadUserLimits()` calling `getConfig()` right after a save (from a *different* process) could read stale data if that process's own poll hadn't fired yet - silently reintroducing the exact staleness bug as an intermittent race. Fixed: added `ConfigService.reloadNow()` (forces an immediate synchronous re-read) called at the top of `syncFromConfig()`/`loadUserLimits()`, and also right after `writeConfig()` in `server.ts` itself (that process's own subsequent reads, e.g. a `GET /config` right after saving, shouldn't lag its own write either).
    4. **Missing input validation**: the new Zerodha `chunkedBuyIndex` branch would silently compute `NaN` for the strike (and resolve a garbage contract) if `niftyLtp` were ever missing - not currently reachable (BulkPcrStrategy is the only caller and always supplies it) but the payload type marks it optional. Added a fail-closed guard.
  - **Resolved (user decision, no code change)**: `syncFromConfig()` unconditionally resets every strategy's `enabled` flag from `config.yml` on every `POST /config` save, which could otherwise silently undo the separate live-only emergency-disable path (`GET /strategies?strategy=X&enable=false`, which only flips the in-memory flag and never writes to the file). User's call: for an emergency stop, use the Config UI's Enabled toggle + Save (the `POST /config` path) instead of the live-only endpoint - it's now equally immediate (via `syncFromConfig`) but also durable, so the gap never actually matters in practice. The live-only `GET /strategies?...&enable=` endpoint itself is untouched/still works, just no longer the recommended way to emergency-stop a strategy.
  - **Follow-up per user instruction**: replaced `ConfigService.watchConfig()`'s `fs.watchFile` (stat-polling, ~5s interval) with `fs.watch` (event-driven/inotify) - user's call, not just for the sync paths above but general config hot-reload responsiveness everywhere `configService.getConfig()` is read across all 4 processes. `reloadNow()` (the forced-reload fix above) is kept regardless, as a deterministic guarantee for the two critical sync call sites rather than relying purely on event delivery.
  - Full design rationale for the original two fixes is at `~/.claude/plans/mossy-jumping-cosmos.md`. `npx tsc --noEmit` is clean (zero real errors) across every touched file after all of the above.
- 2026-09-22: **Code review required before trusting this fix (CLAUDE.md's rule) - not yet run.** Real live trading loss this session (~-15,619, NIFTY2692223400CE via Zerodha): `BulkPcrStrategy`'s target-hit exit called `squareOffOnZerodha`, a blind MARKET order with zero price floor (`market_protection: -1`, no `price` at all) - one second after entry, a single spurious/stale option tick (`ltp=138.6`, real market ~50-52 the entire time per every actual fill before and after) satisfied `ltp >= target`, and the market sell filled at ~51, below the 52.22 entry. Root-caused via `orchestrator.log` + a direct Mongo query (both confirmed the same numbers). Fixed:
  1. **Target-hit exit now places a resting LIMIT sell at the target price** instead of a market order, across all three brokers (Zerodha, Breeze, ANT) per user's explicit scope decision - either locks in the intended profit or simply doesn't fill (user's explicit choice: leave the order open indefinitely on non-fill, no cancel/timeout/market-fallback). New: `Zerodha.placeLimitSellOption`/`placeLimitSellBareOnZerodha`, `placeLimitSellBareOnBreeze` (reuses existing `placeLimitOptionOrder(..., 'sell', ...)`), `ANT.getOrderStatus`+`placeLimitSellBareOnAnt`+new `pendingAntLimitOrders.ts` tracker (ANT had no pending-limit-order infra at all before this). `pendingLimitOrders.ts`/`breezePendingLimitOrders.ts` generalized with an `action: 'Buy'|'Sell'` field (previously hardcoded `'Buy'`). New broker-agnostic `BrokerExecutor.squareOffLimit`/`chunkedOrder.ts`'s `squareOffLimitChunked`/IPC `chunkedSquareOffLimit`, wired the same way `chunkedSquareOff` already was.
  2. **Tick sanity guard**: `processOptionQuote` now ignores (doesn't act on) any single tick priced above `2x entryAvg` before even evaluating the target check - defense-in-depth independent of fix 1, catches the bad tick outright (per user's explicit request).
  3. **`BulkPcrStrategy`'s exit is now async/multi-fill**: since a resting limit order doesn't fill synchronously like the old market order did, `updateTrade` now accumulates each chunk's Sell fill (`soldQty`/`soldValue`) and only runs the "durably disable" completion logic once the full quantity is confirmed sold - previously a no-op (correct only under the old synchronous-market-fill assumption).
  4. **New restart-safety gap this introduced, and its fix**: a restart while a limit-sell exit is still resting could otherwise have `reconcile()` re-derive `phase='holding'` from `getOpenTrades` (the remaining unsold qty still looks "open") and re-arm the target check, risking a SECOND resting sell for the same quantity on the next qualifying tick (an oversell if both eventually filled). Fixed by having `BulkPcrStrategy` persist its own `{phase, heldTsym, heldToken, entryAvg, targetQuantity, soldQty, soldValue}` to a new `bulkPcrStrategyState` Mongo collection on every phase transition, and `reconcile()` now checks this FIRST - if `phase==='selling'`, restores that directly (resuming the fill watch) instead of re-deriving `'holding'`.
  - **Nothing here has been live-verified yet** (implemented with the server stopped, per explicit user instruction not to start it this session):
    - The core fix itself: next time `BulkPcrStrategy` is armed (config currently pinned to `broker: zerodha`, the only broker actually exercised so far), confirm the target-hit exit places a genuine LIMIT sell (not market) and either fills at >= target or rests - watch `orchestrator.log` for `Limit sell chunks resting for ... - waiting for fills` followed by `Exit fill: ... (sold N/13975)` lines accumulating to completion.
    - **Breeze and ANT limit-sell paths are entirely new and unexercised** - Breeze reuses proven machinery (`placeLimitOptionOrder`, `breezePendingLimitOrders.ts`) so risk is low, but ANT's `placeLimitSellBareOnAnt`/`getOrderStatus`/`pendingAntLimitOrders.ts` are brand new against ANT's regular (non-bracket) `placeOrder`/`orders/history` endpoints - lower risk than the bracket-order paths flagged elsewhere in this file (regular `placeOrder`'s `brokerOrderId` field and `orders/history`'s `orderStatus`/`averageTradedPrice` fields are both already "confirmed live" elsewhere in `ANT.ts`), but the new `getOrderStatus` single-shot wrapper itself has not been called live yet.
    - The new restart-mid-exit dedup (`bulkPcrStrategyState` persistence + `reconcile()`'s early `phase==='selling'` branch) needs a real restart while a limit sell is genuinely resting to confirm it actually prevents the double-sell it's designed to prevent, and that `pendingLimitOrders`/`pendingAntLimitOrders`/`breezePendingLimitOrders`'s own independent Mongo-backed restart recovery still delivers the eventual fill back to `updateTrade` correctly after such a restart.
    - `npx tsc --noEmit` is clean (zero errors) since these changes, but that only confirms the code compiles, not that it behaves correctly live - see CLAUDE.md's rule that a review must happen before this is trusted as done.
  - **Update: the `/code-review` skill run against this fix reviewed the wrong scope again** (same failure mode as the 2026-09-21 entry above) - `icici/` isn't a git repo itself, and the skill reviewed `origin/main...HEAD` in the separate mirror (`work/git/stock`), which is 24 unpushed commits of PRE-EXISTING work from prior sessions (never synced via `git.sh`), not this session's actual uncommitted changes. Of its 10 findings, only one was in a file this fix touches and directly relevant - the rest were real but pre-existing bugs in files untouched by this session (`zerodhaExecutor.squareOffOnZerodha`'s missing `pendingSquareOffs` guard, `orderProcess.ts`'s `buyIndex`/`manualBuy` not routing 'breeze', Prism's un-reviewed static-limit sell, `breezeExecutor`'s existing re-price-retry double-sell gap, `prismExecutor`'s missing `brokerOrderId`, `LegManager`/`strategies.ts`/`model.ts` items) - logged here for visibility but out of scope for this fix; worth a real look next time work touches those specific files. The relevant one was fixed immediately: `BulkPcrStrategy.processOptionQuote`'s target-hit check had no `Number.isFinite(quote.ltp)` guard, so a NaN tick (`OptionQuote.fromBreeze` has no presence/NaN guard on `tick.last`, unlike `fromAnt`) would slip past both the target comparison AND the new sanity-multiple guard (`NaN < x` and `NaN > x` are both `false` in JS) and be treated as a genuine target hit - added the explicit finite check as the first line of the function.
  - **Second self-identified gap while reasoning through the review's #1 finding** (not from the review's own scope, but directly caused by this fix): making the exit resting/indefinite instead of near-instant widens the window during which the existing drawdown-breach and NIFTY-expiry-day force-close loops (`orderProcess.ts`) - which market-sell every entry in `bookkeeping.trades` for a user with no awareness of a resting limit order - could double-sell the same still-open quantity. Fixed: both loops now call a new `cancelAnyRestingLimitSell()` (checks all three pending-order trackers' new `findPendingOrdersForSymbol()` lookup, best-effort cancels any resting Sell leg for that user+tsym) immediately before their existing `squareOff()` call. **Not live-verified** - needs a real drawdown-breach or Tuesday-expiry-day force-close to fire while a `BulkPcrStrategy` limit sell is genuinely resting, to confirm the cancel-then-force-close ordering actually prevents the double-sell it's designed to prevent.
  - `npx tsc --noEmit` re-confirmed clean after both of the above fixes.
- 2026-09-22 (later same session): per explicit user instruction ("There should not be any market orders to broker. From user point of view, it can be market order, but get the latest quote and use that and place a limit order"), eliminated every remaining true MARKET order Zerodha could place. Removed `Zerodha.buyOption()` (blind MARKET + `market_protection:-1`) entirely - `src/zerodha/Zerodha.ts`'s `placeLimitBuyOption` is now the only Zerodha buy entry point in the whole app. All 6 call sites (`buyIndexOnZerodha`, `buyContractOnZerodha`/manual buy, `squareOffOnZerodha`, `buyResolvedOnZerodha` - BulkPcrStrategy's chunked-buy path, `marketBuyBareOnZerodha`, `marketSellBareOnZerodha` - LegManager/ContinuousStrategy's bare path) now fetch a live quote via `ANT.getQuote` (the ANT commonToken, confirmed correct per user's own reminder mid-session that ANT/Zerodha use different symbol spaces - never a Zerodha tsym string passed to ANT) and place a marketable LIMIT 1% beyond it (mirrors `antExecutor.ts`'s own existing bracket-order pricing convention exactly) - fills immediately like a market order from the user's point of view, but always carries a real price bound, unlike the blind MARKET order that caused today's earlier ~-15,619 loss. New `zerodhaExecutor.getMarketableZerodhaPrice()` (10s TTL cache keyed by token+direction) - per user's explicit instruction, a CHUNKED order's several same-contract calls (`buyResolvedOnZerodha` called once per chunk by `buyChunked`) reuse one ANT quote fetch instead of hitting AliceBlue's OHLC endpoint (429-rate-limited after 1-2 rapid calls) once per chunk. `squareOffOnZerodha` now requires a matching `bookkeeping.trades` entry (for its ANT token) to price the limit - throws if none is found; every current caller (exitMonitor, drawdown-breach, expiry-day force-close, the generic `/order/squareoff` IPC handler) already operates on a bookkeeping-tracked position, so this should never actually trigger, but flagging in case a future untracked-position squareoff path is added. `npx tsc --noEmit` clean.
  - **Significant new dependency this introduces, not present before**: every Zerodha order (entry AND exit) now hard-depends on ANT's session/quote API being reachable, since Zerodha's own quote/LTP endpoints 403 for this account. Previously only BulkPcrStrategy's PCR check and live ticks needed ANT alongside Zerodha (see `check-broker-sessions` skill's own doc); now EVERY Zerodha-routed strategy's entry and exit does too. If ANT's session is down while Zerodha's is up, Zerodha trading now fails closed entirely (no quote -> `getMarketableZerodhaPrice` throws -> no order placed) rather than degrading to a market order. This is the deliberate, correct trade-off per the user's instruction, but worth remembering when diagnosing "why didn't this Zerodha order go through" - check ANT's session first now, not just Zerodha's.
  - **Not live-verified**: no order has actually been placed through any of the 6 changed call sites since this change - server was kept stopped throughout per earlier instruction this session. Before trusting live: confirm a real Zerodha buy/sell actually fills near-instantly against the 1% marketable buffer (not resting), confirm the existing `Zerodha.getFillPrice` cancel-on-timeout safety net (2026-08-31 fix) still cleanly handles the rare case where it doesn't, and confirm the TTL-cache reuse actually avoids a 429 on a real 8-chunk BulkPcrStrategy entry.
  - **2026-09-24: code review now run** (`e5498bf..HEAD`, covering this change plus everything through today's reconcile fixes) - found and fixed 4 real gaps: `placeOrderWithPendingGuard` (every real buy path) and `antSquareOff`/`breezeSquareOff`/`chunkedSquareOff`/`chunkedSquareOffLimit` weren't awaiting `reconcileInFlight` like `canPlaceOrder`/`squareOff`/`openTrades` already did - reopened the doubled-quantity race on the paths that actually place/close orders; `BulkPcrStrategy.reconcile()`'s no-persisted-exit-state branch was missing the `isPastMarketClose()` guard the parallel 'selling'-phase branch already has; `reconcileBreezePositions` had no timeout on Breeze's HTTP calls even though `reconcileInFlight` now blocks every user's squareOff/emergency-stop on it. All fixed in commit `93b0aba`, `tsc --noEmit` clean, re-reviewed clean on the fix itself (one architectural note below, not a regression).
- 2026-09-24 (later same session): **live incident found and resolved while arming BulkPcrStrategy for both brokers.** User enabled it via the Admin UI mid-session; it gated every attempt with `gate: entry already in progress or done (phase=running)` because `bulkPcrStrategyState`'s persisted Mongo doc had BOTH brokers stuck in `phase='error'` from a same-day 09:15:51 exit-sell attempt that recorded `soldQty:0` against `targetQuantity:13975` for `NIFTY26SEP23400PE` (zerodha) / `NIFTY23400PE` (breeze) - the "manual review required" state `BulkPcrStrategy.reconcile()` deliberately refuses to auto-clear. **Verified via direct read-only `getPositions()` calls against both live broker APIs (not just logs/state) that both are flat** - user separately confirmed the same. Since ground truth showed nothing open, cleared the stale `bulkPcrStrategyState` doc and all 259 `Trade` docs from Mongo so the strategy could arm cleanly. **Nothing pending on this incident itself** - resolved this session, brokers confirmed flat, state cleared, server was stopped throughout the cleanup and restarted fresh by the user afterward.
  - **Architectural note surfaced by the follow-up code review, not fixed (flagging for a decision, not a bug in today's fix):** the `isPastMarketClose()` deferral added above (and the pre-existing one it mirrors, in the 'selling'-phase branch) both assume "the next restart's reconcile() will pick this up" - but `strategiesProcess.ts`'s `runReconcileOnce()` is gated by a module-level flag that only ever fires ONCE per process lifetime (no periodic re-check, no scheduled restart, only event-driven restarts on crash/deploy/hot-reload). If an exit is deferred past market close and no further restart happens before the position needs protection, it could sit unprotected indefinitely, discovered only by chance or a later restart. Worth deciding whether to add a scheduled/periodic reconcile trigger (e.g. a daily restart, or a cron-style re-check) rather than relying purely on event-driven restarts.
