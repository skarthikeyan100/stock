# ToDo

This file is a snapshot of pending/in-progress work, kept current automatically by a SessionEnd hook (see `.claude/settings.json`). It reflects current state, not a historical log.

- Once the market reopens: verify `AntOrderNotifyStream`'s core assumption before relying on it in place of REST polling - place a small ANT order, compare the `brokerOrderId` returned by `ANT.placeOrder`/`placeBracketOrder` against the `norenordno` reported in the order-notify WS push for the same order, and confirm they're the same value. If they don't match, `waitForFill()` will silently time out on every order (see `src/ant/AntOrderNotifyStream.ts` header comment). Also confirm the heartbeat fix (`{"heartbeat":"h","userId":...}`) actually keeps the connection alive past the ~5-minute mark that the earlier wrong format was dropping at, and confirm the fill-price field is really `flprc` (assumed from the Noren/Shoonya convention, not yet seen in a live ANT push).
- `ContinuousStrategy`'s `logGateOnce` gate-log messages are misleading to a reader unfamiliar with "T1" jargon, and specifically `"T1 gate: already ordered / T1 in flight"` reads as if a real broker order exists when it only means the in-memory `ordered` mutex flag is set (e.g. while `isPcrAligned`'s async PCR fetch is in flight, no order has been placed). Reword these gate messages to be self-explanatory and not imply a live order when there isn't one (see `src/strategy/ContinuousStrategy.ts` around `logGateOnce`/`processNiftyQuote`). Not urgent - noted during 2026-08-25 live monitoring session, no code change made yet.
- 2026-08-26: all 24 tasks across the three 2026-08-25 plans (money-correctness, reliability, UX) are implemented, tsc-compiled clean, and committed (mirror `git log` starting at `88c7ff5` through `fc3d134`). Several tasks' own verification is live-only and still needs a real pass once market hours/a live session allow:
  - `AntOrderNotifyStream`'s `brokerOrderId`/`norenordno` assumption (pre-existing item above) - also now covers the squareOffOnAnt fill-price fix, which reuses the same mechanism.
  - `POST /users/:email/settings` with a `broker` value - confirm it reaches `bookkeeping.userSettingsCache` via the order process's IPC socket.
  - `AntStream`'s new reconnect-with-backoff - drop a live connection and confirm `[AntStream] Reconnecting in 2000ms...` then a successful reconnect.
  - `exitMonitor.reconcileFromTrades` - currently a no-op in practice since nothing populates `bookkeeping.trades` from Mongo/broker at order-process startup; re-verify once a startup trade refresh exists.
  - Frontend UX changes (admin Trades/Payments tabs, square-off confirm+error, NotificationBell reconnect) - `tsc --noEmit` and a production build are clean, but no live browser walkthrough was done (Chrome extension wasn't connected this session) - worth a manual pass through `frontend/`'s `npm run dev` before trusting the UI end to end.
